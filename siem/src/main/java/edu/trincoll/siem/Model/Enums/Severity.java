package edu.trincoll.siem.Model.Enums;

public enum Severity {
    Low,    // Matches exactly with database value 'Low'
    Medium, // Matches exactly with database value 'Medium'
    High    // Matches exactly with database value 'High'
}